# Protocol Contracts

Shared schemas and protocol definitions for the public demo.

Initial contracts should cover:

- datasets
- tool calls
- async jobs
- result envelopes
- release handoffs

This package is where future protobuf or JSON Schema definitions should live.

Current executable contracts live in `src/data-contracts.mjs` until the demo graduates to generated schemas.
