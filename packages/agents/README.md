# Agent Policies

Reusable role definitions, prompts, policies, and handoff rules for the public Flion sample.

This package should preserve the existing Flion principle:

```text
implementation cannot self-approve release
```

Agent behavior should be deterministic enough to test with fixtures.
